paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: localhost
        name: student_live
        user: student_live
        pass: 'student_live'
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: localhost
        name: student_dev
        user: student_dev
        pass: 'student_dev'
        port: 3306
        charset: utf8

    testing:
        adapter: mysql
        host: localhost
        name: student_testing
        user: student_testing
        pass: 'student_testing'
        port: 3306
        charset: utf8
